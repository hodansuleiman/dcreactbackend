import React from "react";
import { Link } from "react-router-dom";

const QuizPage = () => {
  return (
    <div>
      <h1>Quiz Page</h1>
      {/*need to work on logic from api */}
    </div>
  );
};

export default QuizPage;
